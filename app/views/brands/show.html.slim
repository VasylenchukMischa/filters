h2 = @brand.name

= link_to "Back", brands_path, class: 'btn btn-primary me-2'
= link_to "Edit", edit_brand_path(@brand), class: 'btn btn-success me-2'
= link_to "Delete", brand_path(@brand), class: 'btn btn-secondary', method: :delete
